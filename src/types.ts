export enum DecayCorrectionValue {
  ADMIN = 'ADMIN',
  START = 'START',
}

export enum UnitsValue {
  CNTS = 'CNTS',
  GML = 'GML',
  BQML = 'BQML',
}

export enum PatientSexValue {
  M = 'M',
  F = 'F',
}

export enum CorrectedImageValue {
  ATTN = 'ATTN',
  DECY = 'DECY',
}
